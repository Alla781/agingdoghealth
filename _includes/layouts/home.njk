---
layout: layouts/base.njk
---
{% set collections = collections or {} %}
{% set collections.articles = collections.articles or [] %}
{% set collections.dogs = collections.dogs or [] %}
<section class="hero">
  <h1>Helping Your Best Friend Age Gracefully</h1>
  <p>Warm, expert guidance for caring owners of senior dogs. Simple tips, practical checklists, and honest advice.</p>
  <a class="btn" href="/articles/">Explore Articles</a>
  <a class="btn btn--light" href="/dogs/">Browse A–Z Conditions</a>
</section>

<section class="grid">
  <div>
    <h2>Popular Guides</h2>
    <ul class="cards">
      {% for post in collections.articles | slice(0,3) %}
      <li class="card">
        <a href="{{ post.url }}">
          <h3>{{ post.data.title }}</h3>
          <p>{{ post.data.excerpt }}</p>
        </a>
      </li>
      {% endfor %}
    </ul>
  </div>
  <div>
    <h2>Common Senior Conditions</h2>
    <ul class="a2z">
      {% for dog in collections.dogs | slice(0,6) %}
      <li><a href="{{ dog.url }}">{{ dog.data.title }}</a></li>
      {% endfor %}
    </ul>
  </div>
</section>
